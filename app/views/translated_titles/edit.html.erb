<h1><%= ts :in, :what => t(:for, :what => t('edit.ing.record', :what => TranslatedTitle.model_name.human.titleize), :whom => "<em>#{h(@category.title)}</em>"), :where => h(@translated_title.language.title %></h1>
<%= error_messages_for :translated_title %>
<%= form_for(:translated_title, :url => category_translated_title_path(@category, @translated_title), :method => :put) do |f| %>
<%=   render :partial => 'form', :locals => {:f => f} %>
      <p><%= f.submit 'Update' %></p>
<%  end %>
<p>
<% if authorized? hash_for_category_translated_title_path(:action => 'destroy') %>
<%=  link_to 'Delete', category_translated_title_path(@category, @translated_title), :confirm => 'Are you sure?', :method => :delete %> |
<% end %>
<%= link_to ts('cancel.this'), @category %>
</p>